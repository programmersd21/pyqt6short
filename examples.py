from pyqt6short import Examples

if __name__ == "__main__":
    # Only instantiate and run examples when explicitly needed
    e = Examples()
    e.run()  # Running all apps simultaneously with multiprocessing
